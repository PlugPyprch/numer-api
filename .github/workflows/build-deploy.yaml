name: numerAPI CI/CD

on:
  push:
    branches: 
      - "main"
      - "master"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Pull repo
        uses: actions/checkout@v3
      - name: Print hello
        run: echo "Hello World" 
        
